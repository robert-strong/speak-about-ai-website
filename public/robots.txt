# Robots.txt for speakabout.ai
# Search engines and AI assistants are explicitly welcome to crawl and cite this site.

User-agent: *
Allow: /
Allow: /_next/static/
Disallow: /api/
Disallow: /admin/
Disallow: /portal/
Disallow: /debug*
Disallow: /test*

# NOTE: /apply and ?source= / ?utm_ URLs are deliberately NOT disallowed here.
# Blocking them let Google index them from links without ever fetching the
# page ("Indexed, though blocked by robots.txt"), so it never saw the
# canonical/noindex tags. Tracking-parameter URLs consolidate through each
# page's self-referencing canonical; /apply carries a noindex meta tag.

# Named groups override the * group, so each named crawler gets the full rule set.
# Search engines
User-agent: Googlebot
User-agent: Bingbot
User-agent: DuckDuckBot
User-agent: Applebot
# OpenAI (ChatGPT search, user-triggered fetches, and training)
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
# Anthropic (Claude)
User-agent: ClaudeBot
User-agent: Claude-User
User-agent: Claude-SearchBot
User-agent: anthropic-ai
# Perplexity
User-agent: PerplexityBot
User-agent: Perplexity-User
# Google AI (Gemini grounding)
User-agent: Google-Extended
# Apple Intelligence
User-agent: Applebot-Extended
# Meta AI
User-agent: Meta-ExternalAgent
# Amazon (Alexa / Rufus)
User-agent: Amazonbot
# Common Crawl (feeds many AI training sets)
User-agent: CCBot
Allow: /
Allow: /_next/static/
Disallow: /api/
Disallow: /admin/
Disallow: /portal/
Disallow: /debug*
Disallow: /test*

# Sitemap location
Sitemap: https://speakabout.ai/sitemap.xml
